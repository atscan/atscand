database:
  path: "atscan.db"
  type: "sqlite"

plc:
  directory_url: "https://plc.directory"
  scan_interval: "10s"
  bundle_dir: "./plc_bundles"
  use_cache: true

pds:
  scan_interval: "15m"
  timeout: "30s"
  workers: 10
  recheck_interval: "5m"

api:
  host: "0.0.0.0"
  port: 8080
